{
  "name": "md-to-pdf",
  "version": "1.0.0",
  "description": "Convert .md files(all files in a directory) to .pdf",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "md-to-pdf": "^5.2.0",
    "merge-pdf-buffers": "^1.0.3"
  }
}
